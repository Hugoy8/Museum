export interface SearchInterface {
    total: number;
    objectIDs: number[];
}

export interface artworkResultSearch {
    id: number,
    title: string,
    artistDisplayName: string,
    objectDate: string,
    primaryImage: string
}